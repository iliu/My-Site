---
title:  Blog Entries
author: Isaac Liu
layout: main
filter: erb
dirty:  true
---
<% prev = nil
   posts = @pages.find(
       :all,
       :content_type => 'tumblog',
       :sort_by => :created_at,
       :reverse => true
   ) %>
   <% posts.each do |page| -%>
    <%= render(:partial => "partials/post", :locals => {:p => page, :c => '', :prev => prev}) %>
  <% if prev != page.created_at.strftime('%Y%m%d') -%>
  <% prev = page.created_at.strftime('%Y%m%d')
     end -%>

<% end -%>

