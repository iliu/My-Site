---
title:  Tag - <%= tag %>
author: Isaac Liu
layout: main
filter: 
  - haml
dirty:  true
sitemap: true
priority: 0.9
changefreq: daily
---
- posts = @pages.find(:all, :content_type => 'tumblog', :sort_by => :created_at, :reverse => true).select{ |p| p.tags && p.tags.include?(<%=tag.inspect%>)}
= render(:partial => 'partials/post_list', :locals => {:posts => posts, :tag => <%=tag.inspect%>})


