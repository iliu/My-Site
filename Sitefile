Webby.site.base = ''
Webby.site.tags_dir = 'tags'

task :default => ['tags:regenerate', :autobuild]

desc 'rebuild with webhost base'
task :build4host do
     `sh .build4host.sh`
end

desc 'deploy the site to the webserver'
task :deploy => ['tags:regenerate', :build, 'deploy:rsync']

# EOF
